# Core scientific and statistical packages
numpy>=1.25,<2.0
scipy>=1.9.0,<2.0

# Messaging and Cloud
pika>=1.3.0                   # RabbitMQ
boto3>=1.28.0,<2.0.0          # AWS SQS

# Logging & Environment
loguru>=0.7.0                 # Optional logging enhancement
python-dotenv>=1.0.0          # Included across repos, even if .env isn't used

# Vault Client (if applicable)
hvac>=1.0.0,<2.0.0            # Only if your VaultClient uses hvac
prometheus_client>=0.17.1
